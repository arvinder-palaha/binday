[project]
name = "binday"
version = "0.1.0"
description = "Next waste collection from a public ICS URL (Home Assistant friendly JSON)"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "icalendar>=6.0.0",
    "recurring-ical-events>=3.8.1",
]
